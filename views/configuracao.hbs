<!DOCTYPE html>
<html lang="pt-br">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard</title>

    <!-- CDN do Tailwind CSS -->
    <script src="https://cdn.tailwindcss.com"></script>

    <!-- CDN do Flowbite -->
    <link href="https://cdn.jsdelivr.net/npm/flowbite@1.4.7/dist/flowbite.min.css" rel="stylesheet">

</head>

<body class="bg-gray-100 text-gray-800 font-sans dark:bg-gray-900 dark:text-white">

<div class="container mx-auto p-6">
    <header class="bg-white dark:bg-gray-800 shadow-lg rounded-lg p-6 mb-6">
        <h1 class="text-3xl font-semibold text-center mb-4">Olá, {{user.username}}!</h1>
        <div class="flex justify-center items-center space-x-4">
            <img src="{{getAvatarUrl user}}" alt="Avatar" class="rounded-full w-24 h-24 border-2 border-gray-300 dark:border-gray-600">
            <div>
                <p class="text-xl">Nome: {{user.username}}</p>
                <p>ID: {{user.id}}</p>
            </div>
        </div>
    </header>

    <div class="mt-6 text-center">
            <a href="/" class="text-blue-500 hover:text-blue-700 dark:text-blue-300 dark:hover:text-blue-400">Voltar</a>
        </div>

</div>


    <!-- CDN do Flowbite JS -->
    <script src="https://cdn.jsdelivr.net/npm/flowbite@1.4.7/dist/flowbite.min.js"></script>

</body>

</html>
