<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login com Discord</title>

    <!-- CDN do Tailwind CSS -->
    <script src="https://cdn.tailwindcss.com"></script>

    <!-- CDN do Flowbite -->
    <link href="https://cdn.jsdelivr.net/npm/flowbite@1.4.7/dist/flowbite.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/flowbite@1.4.7/dist/flowbite.min.js"></script>

    <!-- CDN do Font Awesome para ícones -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" rel="stylesheet">

</head>
<body class="bg-gray-100">

<nav class="bg-white dark:bg-gray-900 fixed w-full z-20 top-0 start-0 border-b border-gray-200 dark:border-gray-600">
  <div class="max-w-screen-xl flex flex-wrap items-center justify-between mx-auto p-4">
      <a href="#" class="flex items-center space-x-3 rtl:space-x-reverse">
          <img src="https://flowbite.com/docs/images/logo.svg" class="h-8" alt="Logo">
          <span class="self-center text-2xl font-semibold whitespace-nowrap dark:text-white">Example Bot</span>
      </a>

      <!-- Verificar se o usuário está logado, se sim, exibe o avatar e nome com dropdown -->
      <div class="flex md:order-2 space-x-3 md:space-x-0 rtl:space-x-reverse" id="user-dropdown-container">
          {{#if user}}
          <div class="relative group">
              <button class="flex items-center space-x-2 text-white bg-blue-700 hover:bg-blue-800 focus:ring-4 focus:outline-none focus:ring-blue-300 font-medium rounded-lg text-sm px-4 py-2 text-center dark:bg-blue-600 dark:hover:bg-blue-700 dark:focus:ring-blue-800">
                  <img src="{{user.avatarUrl}}" alt="Avatar" class="w-8 h-8 rounded-full">
                  <span class="hidden md:block">{{user.username}}</span>
              </button>
              <!-- Dropdown -->
              <div class="absolute right-0 mt-2 bg-white text-black shadow-lg rounded-lg w-48 dark:bg-gray-800 dark:text-white opacity-0 group-hover:opacity-100 transition-opacity duration-300">
                  <ul class="py-2">
                      <li>
                          <a href="/dashboard" class="flex items-center space-x-2 px-4 py-2 text-sm hover:bg-blue-100 dark:hover:bg-gray-700">
                              <i class="fas fa-server"></i>
                              <span>Servidores</span>
                          </a>
                      </li>
                      <li>
                          <a href="/configuracoes" class="flex items-center space-x-2 px-4 py-2 text-sm hover:bg-blue-100 dark:hover:bg-gray-700">
                              <i class="fas fa-cogs"></i>
                              <span>Configurações</span>
                          </a>
                      </li>
                      <li>
                          <a href="/logout" class="flex items-center space-x-2 px-4 py-2 text-sm hover:bg-red-100 text-red-600 dark:hover:bg-gray-700 dark:text-red-400">
                              <i class="fas fa-sign-out-alt"></i>
                              <span>Sair</span>
                          </a>
                      </li>
                  </ul>
              </div>
          </div>
          {{else}}
          <!-- Botão de Login -->
          <a href="/login" class="text-white bg-blue-700 hover:bg-blue-800 focus:ring-4 focus:outline-none focus:ring-blue-300 font-medium rounded-lg text-sm px-4 py-2 text-center dark:bg-blue-600 dark:hover:bg-blue-700 dark:focus:ring-blue-800">Login com Discord</a>
          {{/if}}
      </div>
  </div>
</nav>

<!-- Conteúdo da página -->
<div class="min-h-screen flex items-center justify-center py-10">
    <h1 class="text-3xl font-semibold text-center">Bem-vindo ao nosso sistema</h1>
</div>

</body>
</html>
